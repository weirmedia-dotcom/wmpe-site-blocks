# @weirmedia/blocks

Shared, themeable React/shadcn block components for WMPE client websites. The package is shipped as TypeScript source (no bundler) and consumed by the per-client Next.js renderer via `transpilePackages`, with all colors/typography driven by shadcn CSS variables so a single component set themes per client.
