import type { VariantMap } from "../../types";
import Default from "./Default";
import Lead from "./Lead";
import Narrow from "./Narrow";

const intro_prose: VariantMap = {
  default: Default,
  lead: Lead,
  narrow: Narrow,
};

export default intro_prose;
